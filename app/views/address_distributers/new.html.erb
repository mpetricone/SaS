<%= render partial: 'shared/form_page_title', locals: { title: t(:title_new_for, title: AddressDistributer.model_name.human, for: @distributer.name) } %>
<%= render partial: 'form', locals: { form_action: 'created' } %>
