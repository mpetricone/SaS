<%= render partial: 'shared/index_list', 
    locals: {
    main_model: @addresses, 
    show_fields: [ :street1, :street2, :city, :state, :postal_code, :country, :status],
    delete_msg: t(:prompt_delete_address, item: "#{Address.model_name.human} **"),
    delete_msg_ident: :name_long} %>
