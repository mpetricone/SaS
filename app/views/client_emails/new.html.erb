<%= render partial: 'shared/form_page_title', locals: { title: t(:title_new_for, title: ClientEmail.model_name.human, for: @client.name) } %>
<%= render partial: 'form', locals: { form_action: 'created'} %>
