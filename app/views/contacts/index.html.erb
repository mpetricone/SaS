<%= render partial: 'shared/index_list',
    locals: {
    main_model: @contacts,
    show_fields: [:lname, :fname, :mname, :description ],
    delete_msg: t(:prompt_really_delete, item: Contact.model_name.human),
    delete_msg_ident: :full_name } %>
