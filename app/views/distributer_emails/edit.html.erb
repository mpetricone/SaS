<%= render partial: 'shared/form_page_title', locals: { title: t(:title_edit_for, title: DistributerEmail.model_name.human, for: @distributer.name) } %>
<%= render partial: 'form', locals: { form_action: 'edited' } %>
