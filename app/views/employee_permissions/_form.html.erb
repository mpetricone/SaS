<% render partial: 'shared/error_form_uc', 
    locals: { 
    result: @employee_permission, 
    object_name: EmployeePermission.model_name.human, 
    object_action: form_action } %>
<%= form_for [@employee, @employee_permission] do |f| %>
    <div class='form-cont'>
        <%= render_field_group f, EmployeePermission.human_attribute_name(:permission_id),
            [{label: nil, name: :permission_id, type: :collection_select,
                ext_params: {collection: Permission.all, value_method: :id, text_method: :name}}] %>
    </div>
    <%= render_actions f, @employee %>
<% end %>
