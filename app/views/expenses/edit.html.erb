<%= render partial: 'shared/form_page_title', locals: { title: t(:title_edit, title: "#{Expense.model_name.human} '#{@expense.name}'") } %>
<%= render partial: 'form', locals: { form_action: 'edited' } %>
