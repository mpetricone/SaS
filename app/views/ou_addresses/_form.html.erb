<%= render partial: 'shared/error_form_uc', locals: { result: @ouAddress, object_name: 'Ou Address', object_action: form_action } %>
<%= form_for [@ou, @ouAddress] do |f| %>
  <div class='row'>
        <%= render_address_options f %>
        <%= render_fields_for_address f %>
  </div>
    <%= render_actions f, @ou %>
<% end %>
