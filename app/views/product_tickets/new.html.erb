<%= render partial: 'shared/form_page_title', locals: {
	title: t(:title_add_to_ticket, subject: ProductTicket.model_name.human, client: @ticket.client.name, tid: @ticket.id) } %>
<%= render partial: 'form', locals: { form_action: 'created' } %>
