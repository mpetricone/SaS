<%= render partial: 'shared/index_list',
    locals: {
    main_model: @products,
    show_fields: [:name, :base_cost, :category, :sku, :item_number],
    delete_msg: t(:prompt_really_delete, item: "#{Product.model_name.human} **"),
    delete_msg_ident: :name } %>
