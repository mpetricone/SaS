<%= render partial: 'shared/error_form_uc', locals: { result: @standing, object_action: form_action } %>
<%= form_for(@standing) do |f| %>
  <div class='row'>
    <%= render_field_group f, Standing.human_attribute_name(:name), [{label: nil, name: :name, type: :text_field}] %>
  </div>
  <%= render_actions f, standings_path %>
<% end %>
