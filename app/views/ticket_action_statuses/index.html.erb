<%= render partial: 'shared/index_list' ,
    locals: {
    main_model: @ticket_action_statuses,
    show_fields: [:id, :status],
    delete_msg: t(:prompt_delete_ticket_action_status, item: "#{TicketActionStatus.model_name.human} **", tickets: Ticket.model_name.human(count: 2)),
        delete_msg_ident: :status } %>
