<%= render partial: 'shared/form_page_title', 
locals: {
title: t(:title_new_for, title: TicketNote.model_name.human, 
for: "#{Ticket.model_name.human} #{@ticket.id}" ) } %>
<%= render partial: 'form', locals: { form_action: 'created' }%>

