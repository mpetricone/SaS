<%= render partial: 'shared/form_page_title', locals: {
	title: t(:title_record_payment, client: @ticket.client.name, tid: @ticket.id),
	subtitle: @ticket.short_description } %>
<%= render partial: 'form', locals: {form_action: 'edited' } %>
