<%= render partial: 'shared/form_page_title', locals: { title: t(:title_edit, title: Ticket.model_name.human), subtitle: "#{@ticket.id}"} %>
<%= render partial: 'form', locals: { form_action: 'edited' } %>
