<%= render partial: 'shared/index_list',
    locals: {
    main_model: @work_types,
    show_fields: [:name],
    delete_msg: t(:prompt_really_delete, item: "#{WorkType.model_name.human} **"),
    delete_msg_ident: :name } %>
