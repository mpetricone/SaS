# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% 100.times do |i| %>
client_<%=i%>:
    name: <%= "client_#{i}" %>
    refuse: false
    standing: <%= "standing_#{i}" %>
    default_delivery_address: <%= "address_#{i}" %>
    default_invoice_address: <%= "address_#{i}" %>
<%end%>
