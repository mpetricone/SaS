# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  fname: Admin
  lname: Admin
  mname: Admin
  description: 'Test Admin'
  standing: one

<% 100.times do |i| %>
contact_<%=i%>:
    fname: <%= "Name #{i}" %>
    mname: <%= "MI #{i}" %>
    lname: <%= "Surname #{i}" %>
    description: <%= "descritiption for #{i}" %>
    standing: one
<% end %>


