# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

<% 100.times do |i| %>
  <% 5.times do |j| %>
  ticket_note_<%= "#{i}_#{j}" %>:
    ticket: <%= "ticket_#{i}" %>
    subject: <%= "ticket_note_#{i}_#{j}" %>
    body: <%= "note body #{i} #{j}" %>
  <%end%>
<%end%>
